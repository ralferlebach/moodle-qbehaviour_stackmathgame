# qbehaviour_stackmathgame

Installable Moodle question behaviour plugin for STACK Math Game.

Implemented in this package:
- installable `version.php`;
- `behaviour.php` extending `adaptivemultipart`;
- generated language packs (`en`, `de`);
- added `behaviourtype.php` so Moodle can register the behaviour cleanly.
