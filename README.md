# qbehaviour_stackmathgame

First runnable behaviour adapter for STACK Math Game.

This plugin extends Moodle's `adaptivemultipart` behaviour to provide a stable
DOM contract for the `local_stackmathgame` frontend while preserving native
adaptive controls and feedback in the first implementation stage.
